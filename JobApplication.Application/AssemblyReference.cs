namespace JobApplication.Application
{
    // Marker used only so Program.cs can point MediatR's RegisterServicesFromAssembly
    // at this assembly without referencing a random handler type.
    public sealed class AssemblyReference
    {
    }
}
